# Cursor surface

Cursor rules in `.cursor/rules/` route "ask codex" style requests to:

```bash
node .claude/skills/omega-codex-cli/scripts/ask-codex.mjs "PROMPT"
```

Copy `.claude` and `.cursor` together when moving this setup.
