# Copilot CLI note

This repo centers on Codex CLI headless execution.

If using GitHub Copilot CLI separately, keep it independent from this wrapper. The omega-codex-cli scripts invoke Codex, not Copilot.
