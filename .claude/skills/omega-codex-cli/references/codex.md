# Codex CLI surface

This repo includes `.agents/skills/omega-codex-cli/SKILL.md` to route Codex workflows to the shared wrapper:

```bash
node .claude/skills/omega-codex-cli/scripts/ask-codex.mjs "PROMPT"
```

The canonical instructions live in `.claude/skills/omega-codex-cli/SKILL.md`.
